# TODO

- [ ] Run scheduled `servercopy_cron` jobs on an always-on host. Traditional
  cron requires the host to be awake at each scheduled time and does not replay
  executions missed while the host was asleep.
